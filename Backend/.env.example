# Database Configuration
MONGO_URI=mongodb://localhost:27017/campusqr

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Server Configuration
PORT=5000

# CORS Configuration
CORS_ORIGIN=*

# Node Environment
NODE_ENV=development

# Add these to your .env file if needed

# Expo Configuration (Optional - for advanced features)
# EXPO_ACCESS_TOKEN=your_expo_access_token_here

# Notification Settings
NOTIFICATION_RATE_LIMIT=100
NOTIFICATION_BATCH_SIZE=100

# Database (ensure MongoDB is configured)
MONGODB_URI=mongodb://localhost:27017/school-management

# Server
PORT=5000
JWT_SECRET=your_jwt_secret_here

# Note: Expo Push Notifications work out of the box without additional API keys!
# The Expo SDK handles all the push notification infrastructure for you.
